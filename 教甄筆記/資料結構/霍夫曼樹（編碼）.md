#TeacherSelection 
霍夫曼樹是一種無失真壓縮的編碼樹，根據資料（數字、字元、符號）出現的次數/頻率，做節點順序的安排，概念簡單容易學習。

## 核心機制
1. 把出現次數/頻率最低的兩個資料（包含步驟 2 建立的節點），抓來當葉節點
2. 加總兩個資料的次數/頻率，建立一個父節點
3. 遞迴步驟 1、2. 直到所有資料都被存到樹的葉節點
4. 完成後，從根節點到葉節點的走否路徑，即為它的**霍夫曼編碼**（0 為左、1 為右）