#TeacherSelection 

## Python
### 有趣的 Operator
- `^` operator
	- 用在整數之間是做位元 **XOR**，例如
	  `1 ^ 4 == 5`
	- 用在 set 之間是做**對稱差集**(symmetric difference) 的操作
		- 意指找出兩個集合不同時擁有的元素
		- 例如 ` {1, 2, 3} ^ {3, 4, 5} == {1, 2, 4, 5}`
- 集合 operator
	- `^` 如上
	- `|` 聯集
	- `&` 交集
	- `-` 差集
### 有趣的 function
- zip( )
	- 把多個 iterable 的物件，照順序把對應的元素，壓成一個 iterable 物件
	- 以最短的物件為準，其他物件多出來的元素沒得配對就不壓
	- 不能用 `[index]` 來取值
	- 跑 for 迴圈可以得到被照順序壓好的 tuple，例如： 
	    ``` python
		a = [1, 2, 3]
		b = {4, 5, 6}
		c = {"A": 7, "B": 8, "C": 9, "D": 10}
		
		z = zip(a, b, c)
		
		for item in z:
		  print(item) 
		  # (1, 4, 'A')
		  # (2, 5, 'B')
		  # (3, 6, 'C')
		  # c 字典的 "D" kvp 因為是多出來的，就沒有被壓成 tuple 並輸出
	    ```
	  - 